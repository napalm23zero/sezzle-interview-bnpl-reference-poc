# ┌───────────────────────────────────────────┐
# │  Grafana Dashboard Provisioning           │
# │  Auto-loads dashboards on startup         │
# │  for development use ONLY                 │
# └───────────────────────────────────────────┘

apiVersion: 1

providers:
  - name: 'Pulse Dashboards'
    orgId: 1
    folder: 'Pulse'
    folderUid: 'pulse'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
